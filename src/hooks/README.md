# hooks

Custom hooks de React — lógica reutilizable que varios componentes comparten.

Ejemplos:
- useAuth.js         → saber si el usuario está logueado y obtener sus datos
- useAlbumes.js      → cargar álbumes con paginación y filtros

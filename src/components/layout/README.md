# components/layout

Componentes que definen la estructura visual de todas las páginas.

Ejemplos:
- Navbar.jsx         → barra de navegación superior (logo + links + login)
- Footer.jsx         → pie de página
- PageWrapper.jsx    → envuelve cada página con el Navbar y el Footer

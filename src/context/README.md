# context

Contextos de React — datos globales accesibles desde cualquier componente
sin tener que pasarlos como props de padre a hijo.

Ejemplos:
- AuthContext.jsx    → token JWT, datos del usuario logueado, función de logout
